# 01 Overview

When to read:

- first task in a new cycle,
- whenever project intent is unclear.

Source anchors in `references/DEV_SPEC.md`:

- `## 1.` for project scope and objectives.

Extract these facts:

1. project goal and expected system outcome,
2. non-negotiable constraints,
3. terminology used in later sections.

Use this file to align implementation intent before touching code.
